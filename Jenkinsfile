pipeline {
    agent {
        label "java-slave"
    }
    stages {
        stage("build"){
            steps {
                script {
                    def course = "k8s"
                    println("thanks for entrolling for ${course} course")

                }
            }
        }
    }
}
